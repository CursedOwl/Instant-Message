<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.im.server.mapper.GroupMapper">

    <insert id="insertGroup" parameterType="com.im.server.entity.Group">
        INSERT INTO tbl_group (id, name, account, owner, member) VALUES
        (#{id}, #{name}, #{account}, #{owner}, #{member});
    </insert>
</mapper>